# NLP-codemix-project

We developed the baseline system using the pre-trained multilingual BERT (M-BERT; Devlin et al. (2019)). We use the Hinglish dataset which consists 20k examples. The baseline model implemented the framework and fine tuned the pre-trained BERT-Base.
